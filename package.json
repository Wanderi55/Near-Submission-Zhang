{
  "name": "near-marketplace-contract",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn asp -f unit.spec",
    "build": "asb --target debug",
    "build:release": "asb",
    "asp": "asp --verbose --nologo",
    "deploy": "yarn asb && near deploy --accountId=crossword.wanderi55.testnet --wasmFile=build/release/near-marketplace-contract.wasm"
  },
  "devDependencies": {
    "near-sdk-as": "^3.2.3"
  }
}
